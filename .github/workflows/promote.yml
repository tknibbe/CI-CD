name: promote to staging

on:
  workflow_dispatch:

jobs:
  deploy-staging:
    name: deploy to staging
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: deploy to staging
        run: echo "deploying to staging environment"
      # - name: health check
      #   run: sleep 3 && curl -f http://localhost:${{ vars.PORT }}/health